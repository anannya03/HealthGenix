<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gymtracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.dashboard.FoodTracker"
            android:parentActivityName=".NavigationMainActivity"
            ></activity>
        <activity
            android:name=".ui.dashboard.WorkoutTracker"
            android:parentActivityName=".NavigationMainActivity"></activity>
        <activity
            android:name=".ui.dashboard.TrackWater"
            android:parentActivityName=".NavigationMainActivity" />
        <activity
            android:name=".NavigationMainActivity"
            android:label="DASHBOARD"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>